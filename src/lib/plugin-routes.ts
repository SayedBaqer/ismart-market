// Where each registered plugin's own page lives, keyed by Plugin.slug.
// Shared between the shop nav (individual menu items) and the plugins hub page.
export const PLUGIN_ROUTES: Record<string, string> = {
  'instagram-import': '/shop/plugins/instagram-import',
  'statistics-pro': '/shop/analytics/pro',
}
